name: CI/CD - Build and Push Docker Image

on:
  push:
    branches:
      - master
    tags:
      - 'v*.*.*'
      - 'V*.*.*'
      - '*.*.*'
      - '*.*.*-hf*'
      - '*.*.*-HF*'

env:
  DOCKER_IMAGE_NAME: seu-usuario/mini-mundo
  JAVA_VERSION: '8'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 8
        uses: actions/setup-java@v3
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'temurin'
          cache: 'maven'

      - name: Extract version from tag
        id: extract_version
        run: |
          if [[ $GITHUB_REF == refs/tags/* ]]; then
            VERSION=${GITHUB_REF#refs/tags/}
            # Remove 'v' ou 'V' do início se existir
            VERSION=$(echo $VERSION | sed 's/^[vV]//')
            echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
            echo "IS_TAG=true" >> $GITHUB_OUTPUT
          else
            echo "VERSION=latest" >> $GITHUB_OUTPUT
            echo "IS_TAG=false" >> $GITHUB_OUTPUT
          fi

      - name: Build with Maven
        run: |
          mvn clean package -DskipTests
          echo "Build completed successfully!"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        if: steps.extract_version.outputs.IS_TAG == 'true'
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: |
            ${{ env.DOCKER_IMAGE_NAME }}:${{ steps.extract_version.outputs.VERSION }}
            ${{ env.DOCKER_IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Image published
        if: steps.extract_version.outputs.IS_TAG == 'true'
        run: |
          echo "✅ Docker image published successfully!"
          echo "🐳 Image: ${{ env.DOCKER_IMAGE_NAME }}:${{ steps.extract_version.outputs.VERSION }}"
          echo "🐳 Image: ${{ env.DOCKER_IMAGE_NAME }}:latest"